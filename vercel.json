{
  "version": 2,
  "builds": [
    {
      "src": "api/app.py",  // ← Изменили здесь
      "use": "@vercel/python"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "api/app.py"  // ← И здесь
    }
  ]
}
